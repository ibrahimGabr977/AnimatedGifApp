<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginBottom="5dp"
    android:layoutDirection="ltr"
    android:textDirection="ltr">


    <View
        android:id="@+id/gif_holder_hider_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#73354562"
        android:elevation="3dp"
        android:visibility="gone"
        />


    <ImageView
        android:id="@+id/gif_holder_gif_sign_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/gif_holder_image"
        app:layout_constraintEnd_toEndOf="@id/gif_holder_image"
        app:layout_constraintTop_toTopOf="@id/gif_holder_image"
        app:layout_constraintBottom_toBottomOf="@id/gif_holder_image"
        android:importantForAccessibility="no"
        android:src="@drawable/z_gif_signd_small"
        android:background="@drawable/z_gif_signbg"
        android:elevation="5dp"
        android:visibility="gone"
        android:padding="2.5dp"
        />

    <ImageView
        android:id="@+id/gif_holder_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:visibility="gone"
        android:scaleType="fitXY"
        android:src="@drawable/main_wallpaper"
        />


    <LinearLayout
        android:id="@+id/gif_holder_add_item_text_btn"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/add_newbg"
        android:gravity="center"
        android:visibility="gone"
        >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:textColor="@color/white_color"
            android:textSize="12sp"
            android:textStyle="bold"
            android:text="@string/add_image"
            android:gravity="center"
            app:drawableTopCompat="@drawable/add_newd"
            android:layout_gravity="center"
            android:drawablePadding="3.5dp"
            />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>